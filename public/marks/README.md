# marks

Drop the University of Waterloo lockup here as `waterloo.png` or `waterloo.svg`
(shield plus wordmark, white text, transparent background), then set
`school.mark` in `src/content/resume.ts` to `/marks/waterloo.png`.

Until that is set the name renders as type instead, so nothing is broken and
nothing 404s in the meantime.
